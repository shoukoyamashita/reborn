<% if current_user.liking?(@restaurant) %>
  <%= form_with(model: current_user.mylists.find_by(restaurant_id: @restaurant.id), local: true, method: :delete) do |f| %>
    <%= hidden_field_tag :restaurant_id, @restaurant.id %>
    <%= f.submit 'お気に入りを解除', class: 'btn btn-warning btn-sm' %>
  <% end %>
<% else %>
  <%= form_with(model: current_user.mylists.build, local: true) do |f| %>
    <%= hidden_field_tag :restaurant_id, @restaurant.id%>
    <%= f.submit 'お気に入りに追加', class: 'btn btn-success btn-sm' %>
  <% end %>
<% end %>
