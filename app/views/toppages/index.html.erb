<!--div class="jumbotron"style="background: url(<%= asset_path "hitoyoshi_station.jpg" %>); background-size:cover;"-->

<!--/div-->

<div class="text-center">
  <h3>熊本県人吉市周辺のグルメ情報</h3>
  <br>
    <p>被災から復活したお店のグルメ情報を投稿して<br>街を盛り上げよう。</p>
  <hr>
</div>

<% if logged_in?%>
  <p align=center><%= current_user.name %>さん　いつもご利用ありがとうございます</p>
<% else %>
  <p align=center>
          投稿するにはユーザー登録、ログインが必要です。
          <%= link_to 'ユーザー登録', signup_path, class: 'btn btn-sm btn-outline-primary' %>
          <%= link_to 'ログイン', login_path, class: 'btn btn-sm btn-outline-primary' %>
  </p>
<% end %>
<% @restaurants.each do |restaurant| %>
  <div class="card-header border-info">
    <h4 class="card-title"><%= restaurant.name %></h4>
  </div>
  <div class="card-body">
    <p>
      <button type="button" class="btn btn-outline-dark btn-sm" disabled>営業場所</button>
      　<%= restaurant.place %>
    </p>
    <p>
      <button type="button" class="btn btn-outline-dark btn-sm" disabled>料理・お店について</button>
    　<%= restaurant.content %>
    </p>
    <% if restaurant.photo? %>
      <%= image_tag restaurant.photo.url , width: '80%' %>
    <% end%>
    <p>
      投稿者:<%= link_to restaurant.user.name, user_path(restaurant.user) %> <span class="text-muted">posted at <%= restaurant.created_at %></span>
    </P>
    <%= link_to 'もっと見る', restaurant , class: 'btn btn-sm btn-outline-success' %>　
    <% if current_user == restaurant.user %>
    <%= link_to "この投稿を削除する", restaurant, method: :delete, data: { confirm: "削除してよろしいですか?" }, class: 'btn btn-outline-danger btn-sm' %>
    <% end %>
   </div>
<% end %>